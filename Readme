# Ferramenta Proprietária de Gestão Imobiliária

Este repositório contém uma implementação de referência totalmente **proprietária** da plataforma descrita no documento funcional. O objetivo é oferecer, em um único backend C#/.NET 8, todos os módulos internos necessários para controlar imóveis, negociações, vistorias, manutenção, finanças e comunicação – sem dependências externas com serviços de terceiros.

## 🚀 Visão Geral

A solução está estruturada em camadas:

- **AdministraAoImoveis.Domain** – Entidades ricas, enums e serviços de domínio para disponibilidade de imóveis, pipeline comercial, vistorias, manutenção, tarefas, matrizes de prioridade, documentos, controle financeiro, threads de comunicação, CRM de leads, portal proprietário/interessado, notificações e trilhas de auditoria.
- **AdministraAoImoveis.Application** – DTOs e orquestrações que expõem casos de uso de cada módulo.
- **AdministraAoImoveis.Infrastructure** – Repositórios em memória e composição de dependências para execução imediata sem configuração adicional.
- **AdministraAoImoveis.Api** – API REST minimalista com Swagger cobrindo todos os fluxos proprietários.

> Todos os dados permanecem em memória na execução de referência. Para uso em produção basta substituir os repositórios por implementações persistentes (EF Core, Dapper, etc.) sem alterar as regras de negócio.

## ▶️ Executando

1. Instale o [.NET SDK 8.0](https://dotnet.microsoft.com/download).
2. No diretório raiz, execute:

   ```bash
   dotnet restore
   dotnet run --project src/AdministraAoImoveis.Api/AdministraAoImoveis.Api.csproj
   ```

3. A API sobe com Swagger em `https://localhost:5001/swagger` (ou `http://localhost:5000/swagger`).

## 🧩 Módulos Disponíveis

- **Imóveis** – Cadastro completo, histórico de status, bloqueios automáticos por manutenção ou pendências.
- **Negociações** – Pipeline kanban com controle de etapas, bloqueio único por imóvel e validade de proposta.
- **Vistorias** – Agendamento, conclusão com checklist, geração automática de pendências e integração com manutenção.
- **Pendências e Tarefas** – Controle de SLA, escalonamento e matriz de prioridade (crítico/alto/padrão/oportunidade).
- **Agenda** – Eventos internos ligados a vistorias, negociações e manutenção.
- **Manutenção** – Ordens de serviço com histórico, custos e vínculo direto com imóveis.
- **Financeiro** – Lançamentos proprietários (sinal, caução, aluguel, taxas, manutenção) com bloqueios automáticos de disponibilidade.
- **Documentos e Assinaturas Internas** – Workflows proprietários de geração, coleta de assinaturas e arquivamento.
- **Comunicação Interna** – Threads por contexto (imóvel, negociação, vistoria, manutenção, tarefas, financeiro, lead) com menções e notificações in-app.
- **CRM de Leads** – Captação, atribuição, interação e evolução de estágio sem depender de portais externos.
- **Portais Proprietário/Interessado** – Contas próprias com autenticação, redefinição de senha, papéis e registro de acessos.
- **Notificações** – Mensageria interna com severidade e marcação de leitura.
- **Auditoria** – Registro imutável das ações relevantes em todos os módulos.

## 📡 Endpoints Principais

Todos os recursos possuem rotas REST documentadas automaticamente pelo Swagger. Destaques:

| Domínio | Exemplos de rotas |
| --- | --- |
| Imóveis | `POST /api/properties`, `GET /api/properties/{code}` |
| Negociações | `POST /api/negotiations`, `POST /api/negotiations/{id}/advance` |
| Vistorias | `POST /api/inspections`, `POST /api/inspections/{id}/complete` |
| Tarefas | `POST /api/tasks`, `GET /api/tasks/matrix` |
| Financeiro | `POST /api/financial`, `POST /api/financial/{id}/payment`, `GET /api/financial/blocking` |
| Documentos | `POST /api/documents/workflows`, `POST /api/documents/workflows/{id}/signatures` |
| Comunicação | `POST /api/communications`, `POST /api/communications/{id}/messages` |
| Leads | `POST /api/leads`, `POST /api/leads/{id}/status` |
| Portal | `POST /api/portal/accounts`, `POST /api/portal/authenticate` |
| Notificações | `POST /api/notifications`, `GET /api/notifications/pending/{recipient}` |
| Auditoria | `POST /api/audit`, `GET /api/audit` |

Cada rota opera exclusivamente com dados do próprio sistema, reforçando o caráter proprietário da ferramenta.

## 🗺️ Próximos Passos Sugeridos

- Substituir os repositórios em memória por uma camada de persistência real (SQL ou NoSQL) e storage seguro de arquivos.
- Implementar autenticação JWT, RBAC granular e controle de sessão para os portais interno, proprietário e interessado.
- Adicionar camadas de apresentação (web/mobile) consumindo os endpoints expostos aqui.
- Instrumentar logs, métricas e alertas internos mantendo todos os dados e integrações dentro da infraestrutura da imobiliária.

## ✅ Filosofia Proprietária

- Nenhum módulo depende de serviços externos: toda automatização (assinaturas, notificações, agenda, CRM, financeiro) foi modelada para execução local.
- A arquitetura modular permite expandir funcionalidades mantendo o controle completo sobre dados, fluxos e segurança.
- A implementação serve como base para evolução incremental, preservando o caráter fechado da solução.

